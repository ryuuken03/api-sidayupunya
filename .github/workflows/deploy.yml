name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --production

      # ============================================================
      # PILIH SALAH SATU OPSI DEPLOY DI BAWAH INI
      # Uncomment sesuai hosting yang dipakai
      # ============================================================

      # --- OPSI 1: Deploy ke VPS via SSH ---
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /var/www/antigravity-api
      #       git pull origin main
      #       npm ci --production
      #       npx sequelize-cli db:migrate
      #       pm2 restart antigravity-api

      # --- OPSI 2: Deploy ke Railway ---
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@main
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: antigravity-api

      # --- OPSI 3: Deploy ke Render ---
      # - name: Deploy to Render
      #   run: |
      #     curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

      # --- OPSI 4: Deploy Docker ---
      # - name: Build & Push Docker Image
      #   run: |
      #     docker build -t antigravity-api .
      #     docker tag antigravity-api ${{ secrets.DOCKER_REGISTRY }}/antigravity-api:latest
      #     docker push ${{ secrets.DOCKER_REGISTRY }}/antigravity-api:latest
